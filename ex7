from sys import argv
script,first,second=argv
print"the script is called:",script
print"the first variable is:",first
import numpy as np
np.pi
first=raw_input("r=")
second=2*np.pi*float(first)
print"the second variable is:",second
